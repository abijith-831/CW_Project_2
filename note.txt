f you need to check whether the Redux values (current user info) are the same as the form values before enabling “Update”, you should compare:

✅ form values
with
✅ userInfo (from Redux or fetched profile)

React Hook Form already gives you isDirty, but it marks dirty even if the user types and then puts back the same value.

✅ Best Approach: Use watch() + compare with original data
✔ Step 1: Get all form values using watch()
const { register, handleSubmit, formState: { errors }, setValue, setFocus, watch } = useForm();

✔ Step 2: Create a function that checks if something changed
const formValues = watch();

const isChanged =
  formValues.fullName !== userInfo?.full_name ||
  formValues.nickName !== userInfo?.nick_name ||
  formValues.gender !== userInfo?.gender ||
  formValues.country !== userInfo?.country ||
  formValues.language !== userInfo?.language_preference ||
  formValues.theme !== userInfo?.theme_preference ||
  selectedFile !== null;

✔ Step 3: Use this isChanged for enabling the Update button
<button
  disabled={!isChanged}
  className={`px-4 py-1 rounded-md text-table-header bg-bg-primary opacity-90 
    ${!isChanged ? "opacity-50 cursor-not-allowed" : "hover:scale-102"} 
    duration-300 transition-transform`}
>
  Update
</button>

✅ Full working example only for the button section
const formValues = watch();

const isChanged =
  formValues.fullName !== userInfo?.full_name ||
  formValues.nickName !== userInfo?.nick_name ||
  formValues.gender !== userInfo?.gender ||
  formValues.country !== userInfo?.country ||
  formValues.language !== userInfo?.language_preference ||
  formValues.theme !== userInfo?.theme_preference ||
  selectedFile !== null;

{isEdit && (
  <div className='flex items-end justify-end gap-10 mb-4'>
    
    <button
      onClick={() => setIsEdit(false)}
      className='px-4 py-1 rounded-md text-secondary bg-neutral-300 hover:scale-102 duration-300 transition-transform'
    >
      Cancel
    </button>

    <button
      disabled={!isChanged}
      className={`px-4 py-1 rounded-md text-table-header bg-bg-primary opacity-90
        ${!isChanged ? "opacity-50 cursor-not-allowed" : "hover:scale-102"} 
        duration-300 transition-transform`}
    >
      Update
    </button>

  </div>
)}

🎉 Now the “Update” button will only enable when:

✔ User actually changed something
✔ Or a new profile image is selected
✔ Putting back the old value makes the button disabled again

If you want, I can prepare a clean reusable hook like:

useIsFormChanged(formValues, userInfo);